# HL_Blobox ワイルドカード一覧

テキスト設定の「テキスト内容」で使用できるワイルドカードの一覧です。

---

## 基本形式

ワイルドカードは `$[ワイルドカード名]` の形式で記述します。

```
例: X:$[box_x_position], Y:$[box_y_position]
```

ワイルドカード内では**四則演算**（`+` `-` `*` `/` `( )`）を使用できます。

```
例: $[box_x_position + box_width / 2]  → ボックスの中央 X 座標
     $[box_y_position - 10]            → Y 座標から 10px 引いた値
```

---

## ボックスの数値・情報

| ワイルドカード | 説明 | 表示例 |
|---|---|---|
| `box_x_position` | ボックスの X 座標 | `120` |
| `box_y_position` | ボックスの Y 座標 | `340` |
| `box_width` | ボックスの幅 | `320` |
| `box_height` | ボックスの高さ | `180` |
| `box_id` | ボックスの内部 ID（1 始まり） | `1` |
| `box_pixel_count` | ボックスの総ピクセル数 | `4821` |
| `box_pixel_percent_{有効数字}` | 画面全体に対するボックスのピクセル数の割合（%） | `3.7` |

> `box_pixel_percent_{有効数字}` の有効数字は 0〜5 を指定します（例: `box_pixel_percent_1` → `3.7`）。

---

## 色

| ワイルドカード | 説明 | 表示例 |
|---|---|---|
| `box_color_rgb` | ボックス中心の色（R,G,B / 各 0〜255・3桁パディング） | `255,128,000` |
| `box_color_rgb_hex_upper` | ボックス中心の色（16進数・大文字） | `FF8000` |
| `box_color_rgb_hex_lower` | ボックス中心の色（16進数・小文字） | `ff8000` |
| `box_color_r` | 赤成分（0〜255） | `255` |
| `box_color_g` | 緑成分（0〜255） | `128` |
| `box_color_b` | 青成分（0〜255） | `0` |
| `box_color_r_hex_upper` | 赤成分（16進数・大文字） | `FF` |
| `box_color_g_hex_upper` | 緑成分（16進数・大文字） | `80` |
| `box_color_b_hex_upper` | 青成分（16進数・大文字） | `00` |
| `box_color_r_hex_lower` | 赤成分（16進数・小文字） | `ff` |
| `box_color_g_hex_lower` | 緑成分（16進数・小文字） | `80` |
| `box_color_b_hex_lower` | 青成分（16進数・小文字） | `00` |

---

## ランダム

| ワイルドカード | 説明 | 表示例 |
|---|---|---|
| `random_int_{min,max}` | `min`〜`max` の範囲のランダムな整数 | `42` |
| `random_float_{min,max}` | `min`〜`max` の範囲のランダムな小数 | `3.14` |
| `random_bool` | ランダムな真偽値 | `True` / `False` |
| `random_choice_{候補1,候補2,...}` | 候補の中からランダムに選択 | `b` |
| `random_alphabet_{長さ}` | ランダムな英字（大文字・小文字混在） | `aBcD` |
| `random_alphabet_upper_{長さ}` | ランダムな英字（大文字のみ） | `ABCD` |
| `random_alphabet_lower_{長さ}` | ランダムな英字（小文字のみ） | `abcd` |
| `random_alphanumeric_{長さ}` | ランダムな英数字（大文字・小文字混在） | `aB3d` |
| `random_alphanumeric_upper_{長さ}` | ランダムな英数字（大文字のみ） | `A1B2` |
| `random_alphanumeric_lower_{長さ}` | ランダムな英数字（小文字のみ） | `a1b2` |
| `random_special_{長さ}` | ランダムな記号 | `!@#` |
| `random_hex_{長さ}` | ランダムな 16 進数 | `a1f2` |
| `random_string_{長さ}` | ランダムな文字列（英数字・記号混在） | `a!B3` |

---

## 四則演算

数値系のワイルドカードは四則演算で組み合わせることができます。

**使用できる演算子**: `+` `-` `*` `/` `( )`

**利用できる変数**:

- `box_x_position`
- `box_y_position`
- `box_width`
- `box_height`
- `box_color_r`
- `box_color_g`
- `box_color_b`
- `box_pixel_count`
- `box_id`

### 使用例

| テンプレート | 計算内容 |
|---|---|
| `$[box_x_position + box_width / 2]` | ボックスの中央 X 座標 |
| `$[(box_width + box_height) * 2]` | ボックスの周囲長 |
| `$[box_y_position - 10]` | Y 座標から 10px 引いた値 |
| `$[box_pixel_count / (box_width * box_height)]` | ボックス内の占有率 |

---

## 使用例（テキスト内容）

| テキスト内容 | 表示結果の例 |
|---|---|
| `X:$[box_x_position],Y:$[box_y_position]` | `X:120,Y:340` |
| `$[box_width]x$[box_height]` | `320x180` |
| `No.$[box_id]` | `No.1` |
| `色: $[box_color_rgb]` | `色: 255,128,000` |
| `ピクセル: $[box_pixel_count]` | `ピクセル: 4821` |
| `割合: $[box_pixel_percent_1]%` | `割合: 3.7%` |
| `ID: $[random_int_100,999]` | `ID: 482` |
| `名前: $[random_alphabet_6]` | `名前: aBcDeF` |
